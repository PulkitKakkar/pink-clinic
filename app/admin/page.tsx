import Link from "next/link";
import { ArrowRight, FileText, FlaskConical, Users } from "lucide-react";
import { AdminHeader } from "@/components/admin/admin-header";
import { consultationTemplates } from "@/lib/admin/templates";
import { getConsultations } from "@/lib/admin/storage";

export const dynamic = "force-dynamic";

export default async function AdminDashboard() {
  const records = await getConsultations();
  return <><AdminHeader /><main className="mx-auto max-w-7xl px-5 py-8 sm:px-8 sm:py-12"><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.2em] text-pink">Staff workspace</p><h1 className="mt-2 font-display text-5xl">Consultations</h1><p className="mt-3 max-w-xl text-sm leading-6 text-black/45">Create and review staff-completed consultation records. This prototype is restricted to local test data.</p></div><div className="rounded-2xl border border-amber-200 bg-amber-50 px-4 py-3 text-xs leading-5 text-amber-900"><span className="flex items-center gap-2 font-bold"><FlaskConical size={15} /> Test environment</span>No real customer data</div></div><div className="mt-8 grid gap-3 sm:grid-cols-3"><article className="rounded-2xl bg-white p-5 shadow-soft"><FileText className="text-pink" size={20} /><strong className="mt-5 block text-3xl">{consultationTemplates.length}</strong><span className="text-xs text-black/40">Form templates</span></article><article className="rounded-2xl bg-white p-5 shadow-soft"><Users className="text-pink" size={20} /><strong className="mt-5 block text-3xl">{records.length}</strong><span className="text-xs text-black/40">Test records</span></article><article className="rounded-2xl bg-white p-5 shadow-soft"><FlaskConical className="text-pink" size={20} /><strong className="mt-5 block text-3xl">Local</strong><span className="text-xs text-black/40">Storage mode</span></article></div><section className="mt-10"><h2 className="font-display text-3xl">Start a consultation</h2><div className="mt-5 grid gap-3 md:grid-cols-2 lg:grid-cols-3">{consultationTemplates.map((template) => <Link key={template.slug} href={`/admin/consultations/${template.slug}`} className="group rounded-2xl border border-black/5 bg-white p-5 shadow-soft transition hover:-translate-y-1"><p className="text-[9px] font-bold uppercase tracking-[.18em] text-pink">Clinical review required</p><h3 className="mt-3 font-display text-2xl leading-none">{template.title}</h3><p className="mt-3 text-xs leading-5 text-black/45">{template.description}</p><span className="mt-5 flex items-center gap-2 text-xs font-bold text-pink">Open form <ArrowRight size={14} /></span></Link>)}</div></section><section className="mt-10"><h2 className="font-display text-3xl">Recent test records</h2><div className="mt-5 overflow-hidden rounded-2xl border border-black/5 bg-white">{records.length ? records.slice(0, 10).map((record) => <div key={record.id} className="flex items-center justify-between gap-4 border-b border-black/5 p-4 last:border-0"><div><p className="text-sm font-bold">{String(record.answers.fullName || "Unnamed test client")}</p><p className="mt-1 text-xs text-black/40">{record.templateTitle}</p></div><time className="text-[10px] uppercase tracking-[.12em] text-black/35">{new Date(record.createdAt).toLocaleString("en-GB")}</time></div>) : <p className="p-5 text-sm text-black/40">No test consultations saved yet.</p>}</div></section></main></>;
}
