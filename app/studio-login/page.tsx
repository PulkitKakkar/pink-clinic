import Image from "next/image";

export default async function StudioLoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) {
  const params = await searchParams;
  return <main className="grid min-h-screen place-items-center bg-[#210013] px-5 py-12"><div className="w-full max-w-md rounded-[2rem] bg-white p-7 shadow-luxe sm:p-10"><Image src="/images/pink-logo.jpeg" alt="Pink Beauty" width={160} height={80} className="mx-auto h-auto w-32" /><p className="mt-8 text-center text-[10px] font-bold uppercase tracking-[.22em] text-pink">Content administrators only</p><h1 className="mt-2 text-center font-display text-4xl">Studio sign in</h1><p className="mt-3 text-center text-xs leading-5 text-black/45">Manage products, prices, collections and website content. Staff admin credentials do not provide access.</p>{params.error && <p className="mt-5 rounded-xl bg-red-50 p-3 text-center text-xs font-bold text-red-700">Incorrect Studio administrator email or password.</p>}<form action="/api/studio-auth/login" method="post" className="mt-7 grid gap-4"><label className="grid gap-2 text-xs font-bold">Studio administrator email<input name="email" type="email" required autoComplete="username" className="rounded-xl border border-black/10 bg-cream px-4 py-3 outline-none focus:border-pink" /></label><label className="grid gap-2 text-xs font-bold">Password<input name="password" type="password" required autoComplete="current-password" className="rounded-xl border border-black/10 bg-cream px-4 py-3 outline-none focus:border-pink" /></label><button className="button-primary mt-2 w-full" type="submit">Open Content Studio</button></form>{process.env.NODE_ENV !== "production" && <div className="mt-6 rounded-xl border border-amber-200 bg-amber-50 p-4 text-xs leading-5 text-amber-900"><strong>Local Studio test account</strong><br />studio@pinkbeauty.test<br />PinkStudio2026!</div>}</div></main>;
}
