import { Star } from "lucide-react";
import { reviews } from "@/lib/content";

export function Reviews() {
  return <section className="section-shell bg-white"><div className="container-site"><div className="section-header text-center"><p className="mx-auto mb-3 justify-center text-[10px] font-bold uppercase tracking-[.3em] text-pink sm:mb-4 sm:text-[11px]">Client stories</p><h2 className="section-title">Beautiful words from<br />our community.</h2></div><div className="grid gap-3 sm:gap-5 lg:grid-cols-3">{reviews.map(review => <article key={review.name} className="rounded-2xl border border-black/5 bg-cream p-5 sm:rounded-[1.5rem] sm:p-8"><div className="flex text-[#f4b638]">{[1,2,3,4,5].map(i => <Star key={i} size={13} fill="currentColor" />)}</div><blockquote className="mt-4 font-display text-xl leading-[1.25] tracking-tight sm:mt-6 sm:text-2xl">“{review.text}”</blockquote><div className="mt-5 flex items-center gap-3 sm:mt-8"><span className="grid h-9 w-9 place-items-center rounded-full bg-pink text-[10px] font-bold text-white sm:h-10 sm:w-10 sm:text-xs">{review.initials}</span><div><p className="text-xs font-bold sm:text-sm">{review.name}</p><p className="text-[9px] uppercase tracking-[.14em] text-black/40 sm:text-[10px]">{review.service} · Google review</p></div></div></article>)}</div></div></section>;
}
