import Image from "next/image";

export function PageHero({ eyebrow, title, copy, image, compact = false }: { eyebrow: string; title: string; copy: string; image: string; compact?: boolean }) {
  return <section className={`relative overflow-hidden bg-[#210013] text-white ${compact ? "pt-28 sm:pt-32" : "pt-24 sm:pt-32"}`}><Image src={image} alt="" fill priority className="object-cover opacity-45" sizes="100vw" /><div className="absolute inset-0 bg-gradient-to-r from-[#210013] via-[#210013]/75 to-pink/20" /><div className={`container-site relative flex ${compact ? "pb-6 sm:pb-8" : "pb-8 sm:pb-14"}`}><div className="max-w-3xl"><p className={`text-[10px] font-bold uppercase text-pink-light ${compact ? "mb-4 inline-flex rounded-full border border-pink-light/30 bg-pink/20 px-3 py-1.5 tracking-[.2em]" : "mb-3 tracking-[.3em] sm:mb-5"}`}>{eyebrow}</p><h1 className={`font-display leading-[.92] tracking-[-.055em] ${compact ? "max-w-2xl text-5xl sm:text-6xl lg:text-7xl" : "text-5xl sm:text-8xl"}`}>{title}</h1><p className={`max-w-xl text-sm leading-6 text-white/65 sm:text-base sm:leading-7 ${compact ? "mt-4 sm:mt-5" : "mt-4 sm:mt-6"}`}>{copy}</p></div></div></section>;
}
